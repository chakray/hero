import { ElementRef } from '@angular/core';
import { Router } from '@angular/router';
export declare class CmEnrouteAttr {
    private el;
    private router;
    baseUrl: string;
    constructor(el: ElementRef, router: Router);
    onClick(event: any): void;
}
